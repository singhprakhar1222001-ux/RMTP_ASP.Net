Distributed Work Management System

A production-inspired, event-driven distributed work management platform built with .NET, designed to demonstrate backend engineering principles used in modern cloud-native systems. The project focuses on reliability, scalability, eventual consistency, and clean service boundaries rather than implementing a simple CRUD application.

Architecture

The platform follows a microservices architecture where each service owns its data and communicates asynchronously through integration events.
